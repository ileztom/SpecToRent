spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spectorrent
    username: spectorrent
    password: spectorrent
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

server:
  port: 8080

logging:
  level:
    org.springframework: INFO
